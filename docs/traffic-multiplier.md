# 节点名里的【3x】是什么意思？——流量倍率避坑指南

买机场 / 住宅 IP 之前，先学会看这一个数字。它决定你实际能用多少流量，而**大部分服务商不会在购买页写它**。

---

## 一句话

节点名前面的 `【3x】`、`[2x]`、`x1.5` 是**流量倍率**：走这条线路时，**跑 1GB 从你的额度里扣 3GB**。

倍率 1.0（或者不标）= 跑 1GB 扣 1GB，这才是 1:1。

## 为什么服务商要设倍率

不是坑人，本身有合理原因：中转线路要额外买一台机器转发，一份流量在中转机上进出各算一次，成本天然高于直连。所以**倍率本身不是问题，不写在购买页才是问题**。

## 算给你看

假设一个套餐标称 **800GB / 月**：

| 你实际用的节点 | 倍率 | 真正能跑多少 |
|---|---|---|
| 直连节点 | 1x | 800 GB |
| 中转节点 | 2x | 400 GB |
| 中转节点 | 3x | **267 GB** |

如果一个套餐的**好线路全部是 3x**，而只有慢的、经常超时的那几条是 1x，那你实际拿到的就是 267GB，不是 800GB。

换算成每 GB 单价，差别是 **3 倍**。

## 怎么自己查（3 步，不用信任何广告）

**第 1 步：看节点名。**
导入订阅后，直接看节点列表。倍率通常写在名字最前面：

```
【3x】中转|日本樱花        ← 3 倍
[2x] 香港 BGP             ← 2 倍
美国 AT&T 家宽 hy2         ← 没标，通常是 1x
```

**第 2 步：如果名字没标，实测。**

1. 打开面板，记下当前「已用流量」
2. 连上要测的那条节点
3. 下载一个**大小已知**的文件（比如 1GB 测试文件）
4. 刷新面板，看已用流量涨了多少

涨 1GB = 1x。涨 3GB = 3x。

> 用 `curl` 更准，因为你能确认下了多少字节：
> ```bash
> curl -o /dev/null -w "实际下载 %{size_download} bytes\n" \
>   https://speed.cloudflare.com/__down?bytes=1000000000
> ```

**第 3 步：换一条不同的节点再测一次。** 同一个套餐里，直连和中转的倍率经常不一样。测一条不代表全部。

## 买之前该问什么

把这三句发给客服，答不上来的自己掂量：

1. 「你们所有节点的流量倍率分别是多少？」
2. 「购买页写的 800GB，是按 1x 算的还是标称额度？」
3. 「哪些节点是 1x？」

## 关于易联（Voguesly）

写这篇不是为了绕回来推销，但既然讲到这里，把我们自己的数据摆出来让你验：

**易联的直连与 CF 线路倍率 = 1.00；经中转的线路会在节点名标明【3x】或【5x】，标几倍就扣几倍，没有「不标但偷偷多扣」的节点。**

你不用信这句话，导入订阅后按上面第 2 步逐条测：没标的，涨 1GB 扣 1GB；标【3x】的，涨 1GB 扣 3GB。我们自己也用同样的方法测过，标 3x 的实扣 3.00 倍，标 5x 的实扣 5.00 倍。

中转线路的成本确实高，换来的是晚高峰更稳。我们选择把倍率明明白白写在节点名上，让你自己决定哪些流量值得走中转，而不是藏在一个看不见的地方。

---

## 相关

- [美国住宅 IP 代理指南（主页）](../README.md)
- [机房 IP、VPS、住宅 IP 到底有什么区别？](https://voguesly.com/guides/datacenter-vs-residential-ip/)
- [怎么确认自己用的是不是真住宅 IP？](https://voguesly.com/guides/how-to-verify-residential-ip/)
